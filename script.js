function render(){let s=document.getElementById('search').value.toLowerCase(),g=document.getElementById('genre').value,a=GAMES.filter(x=>(g==='All genres'||x[1]===g)&&x[0].toLowerCase().includes(s));document.getElementById('gameGrid').innerHTML=a.map((x,i)=>`<article class="game-card"><a href="game.html?name=${encodeURIComponent(x[0])}" target="_blank"><img src="${x[2]}" alt="${x[0]}" loading="lazy"><div class="game-info"><small>${x[1]}</small><h3>${x[0]}</h3><p>${x[4]}</p></div></a><a class="store" href="${x[3]}" target="_blank" rel="noopener">Steam / Official ↗</a></article>`).join('')||'<p>No games found.</p>'}
document.getElementById('search').oninput=render;document.getElementById('genre').onchange=render;
let ranks=GAMES.slice(0,24);document.getElementById('rankStrip').innerHTML=ranks.map((x,i)=>`<a href="game.html?name=${encodeURIComponent(x[0])}" target="_blank"><b>#${String(i+1).padStart(2,'0')}</b>${x[0]}</a>`).join('');
const panel=document.getElementById('toolsPanel'),overlay=document.getElementById('overlay');document.getElementById('toolsBtn').onclick=()=>{panel.classList.add('open');overlay.classList.add('show')};document.getElementById('closeTools').onclick=close;overlay.onclick=close;function close(){panel.classList.remove('open');overlay.classList.remove('show')}
render();